{
  "name": "frigo",
  "version": "1.0.0",
  "private": true,
  "description": "Chambre froide documentaire — coffre-fort personnel de documents professionnels",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client install && npm --prefix client run build",
    "hash-password": "node scripts/hash-password.js",
    "totp-secret": "node scripts/totp-secret.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@vercel/blob": "^2.6.1",
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.15.0",
    "otplib": "^12.0.1",
    "pdf-parse": "^2.4.5",
    "zod": "^3.24.0"
  }
}
